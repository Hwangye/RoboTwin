# assets/embodiments/.gitattributes routes *.yml/*.stl/*.urdf through Git LFS;
# GitHub refuses new LFS objects on public forks. `!` resets the attributes to
# unspecified (default text handling), unlike `-` which would force binary diff.
* !filter !diff !merge !text
*.stl binary
